#include "../includes/minirt.h"

double atod(char *str)
{
    double minus;
    double itg;
    double frct;

    if (*str == '-')
        minus = -1;
    //　未実装
    printf("str: %s\n", str);
    return (0.2);
}
